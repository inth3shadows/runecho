"""Brackets appear BEFORE __all__, and __all__ is a tuple (requests-shaped)."""
import os

_HOME = os.path.expanduser("~")
_TABLE = {"a": [1, 2]}

try:
    import ssl
except ImportError:
    ssl = None

if os.name == "posix":
    PLATFORM_FLAG = True
else:
    PLATFORM_FLAG = False

__all__ = (
    "get",
    "post",
)


def get(url):
    local_only = 1
    return local_only


def post(url):
    ...
