# Star-imports a module that is not installed: the chain cannot be followed, so
# the whole package must abstain rather than report a set missing those names.
from nowhere_at_all import *

def local(): ...
