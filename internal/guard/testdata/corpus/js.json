[
  {
    "name": "js-tp-hallucinated-call",
    "desc": "bare call to a nonexistent symbol must be flagged",
    "file": "submit.js",
    "known": ["saveUser"],
    "edit": "function handleSubmit(form) {\n  const result = processOrder(form);\n  saveUser(result);\n  return result;\n}\n",
    "expect": ["processOrder"]
  },
  {
    "name": "js-tp-typo-with-suggestion",
    "desc": "one-edit typo of a known symbol is flagged with a did-you-mean",
    "file": "refresh.js",
    "known": ["fetchUsers"],
    "edit": "function refresh() {\n  return fetchUser().then(render);\n}\n",
    "expect": ["fetchUser"],
    "suggest": {"fetchUser": "fetchUsers"}
  },
  {
    "name": "js-tn-builtins-and-qualified",
    "desc": "builtins (parseInt, fetch) and qualified calls (console.log, JSON.parse) are clean",
    "file": "parse.js",
    "edit": "function readPort(raw) {\n  console.log(raw);\n  const cfg = JSON.parse(raw);\n  return parseInt(cfg.port, 10);\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-arrow-def-then-call",
    "desc": "arrow function defined in the edit may be called in the edit",
    "file": "fmt.js",
    "edit": "const fmtDate = (d) => d.toISOString();\nconst when = fmtDate(new Date());\n",
    "expect": []
  },
  {
    "name": "js-tn-esm-import-fold",
    "desc": "names bound by import statements in the on-disk file are known",
    "file": "register.js",
    "infile": "import { validateUser, normalizeEmail } from './auth';\nimport config from './config';\n",
    "edit": "function registerUser(form) {\n  const email = normalizeEmail(form.email);\n  return validateUser(email);\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-require-fold",
    "desc": "destructured require bindings in the on-disk file are known",
    "file": "cli.js",
    "infile": "const { parseArgs } = require('util');\n",
    "edit": "function main(argv) {\n  const opts = parseArgs(argv);\n  return opts;\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-template-literal-sql",
    "desc": "SQL keywords inside template literals must not read as calls",
    "file": "query.js",
    "edit": "function countLogs(db, level) {\n  const q = `SELECT COUNT(*) FROM logs WHERE level = ${level}`;\n  return db.query(q);\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-dynamic-callable",
    "desc": "a variable bound to a callable (computed member access) is a local binding, so a bare call to it resolves — folded via LocallyBoundNames (was a tracked FP before that fold-in)",
    "file": "dispatch.js",
    "edit": "function dispatch(handlers, evt) {\n  const fn = handlers[evt.type];\n  fn(evt);\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-usestate-setter-in-hunk",
    "desc": "a useState destructured setter defined and called within the edit resolves via the LocallyBoundNames fold (the largest real-world FP cluster)",
    "file": "Field.tsx",
    "edit": "import { useState } from 'react';\nfunction Field() {\n  const [text, setText] = useState('');\n  setText('hi');\n  return text;\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-usestate-setter-outside-hunk",
    "desc": "a useState setter bound on an UNTOUCHED line resolves via the whole-file LocallyBoundNames fold (the common hook-mode case: the edited hunk calls setText, the destructure lives elsewhere)",
    "file": "useLabel.ts",
    "infile": "import { useState } from 'react';\nfunction useLabel() {\n  const [text, setText] = useState('');\n  return { text, setText };\n}\n",
    "edit": "function reset() {\n  setText('');\n}\n",
    "expect": []
  },
  {
    "name": "js-tn-test-globals",
    "desc": "test-runner ambient globals (describe/it/expect/beforeEach/afterEach) resolve inside a spec file",
    "file": "math.test.ts",
    "edit": "describe('math', () => {\n  beforeEach(() => {});\n  afterEach(() => {});\n  it('adds', () => {\n    expect(1 + 1).toBe(2);\n  });\n});\n",
    "expect": []
  },
  {
    "name": "js-tp-test-global-in-product",
    "desc": "the test-global gate is scoped: a bare describe() in a NON-test file is still flagged, so a hallucinated call is not masked by the test-runner allowlist",
    "file": "math.ts",
    "edit": "export function check() {\n  return describe(1);\n}\n",
    "expect": ["describe"]
  },
  {
    "name": "js-export-function-decl",
    "desc": "`export function Name(` is recognized as a definition, so the self-reference is not flagged",
    "file": "report.js",
    "edit": "export function buildReport(rows) {\n  return rows.length;\n}\n",
    "expect": []
  },
  {
    "name": "js-async-function-decl",
    "desc": "`async function Name(` is recognized as a definition, so the self-reference is not flagged",
    "file": "load.js",
    "edit": "async function loadConfig() {\n  return {};\n}\n",
    "expect": []
  },
  {
    "name": "js-fp-regex-literal",
    "desc": "KNOWN FP (by audit decision): a regex literal `/Foo(x)/` extracts Foo as a bogus call. Line-level regex-vs-division disambiguation is genuinely ambiguous (a leading `/` could be either), so this stays a tracked false positive rather than risking a heuristic that drops real division-context calls.",
    "file": "match.js",
    "edit": "function match(s) {\n  const re = /Foo(x)/;\n  return re.test(s);\n}\n",
    "expect": [],
    "known_fp": ["Foo"]
  },
  {
    "name": "js-tn-standard-globals",
    "desc": "Standard JS globals (URI encoders, base64, crypto, typed arrays, Intl, Function) are builtins, not hallucinated calls",
    "file": "encode.js",
    "edit": "function pack(s) {\n  const u = encodeURI(s) + decodeURI(s);\n  const b = btoa(s) + atob(s);\n  const id = crypto.randomUUID();\n  const buf = new Uint8Array(8);\n  const view = new DataView(new ArrayBuffer(8));\n  const fmt = Intl.NumberFormat();\n  const fn = Function(\"return 1\");\n  return [u, b, id, buf, view, fmt, fn];\n}\n",
    "expect": []
  }
]
